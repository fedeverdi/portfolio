{
  "name": "myportfolio-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 4000",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply myportfolio-db --local",
    "db:migrate:prod": "wrangler d1 migrations apply myportfolio-db --remote",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240910.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.70.0"
  }
}
